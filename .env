# Foliage App Configuration

# Web server port
PORT=8080

# PostgreSQL connection (adjust as needed)
DATABASE_URL=host=localhost user=postgres password=postgres dbname=foliage port=5432 sslmode=disable
